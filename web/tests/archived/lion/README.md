# Previous landing hero

The lion component and its artwork are retained as an alternate design. These homepage-specific browser tests are outside the active `tests/ui` directory because the homepage now mounts the editorial instrument. The current homepage's key, dial, power, source-content, fallback, reduced-motion, mobile and remount contracts live in `tests/ui/pocket-instrument.spec.ts` and `tests/ui/mobile-landing.spec.ts`.

Restore these tests alongside the lion hero if that design is reinstated.
