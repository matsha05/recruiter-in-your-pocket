export const workspaceTrustMessage = {
  title: "Before you run",
  body:
    "Your upload is encrypted in transit. Anonymous reviews are not attached to an account unless you choose to sign in and save them.",
  detail:
    "Signed-in reviews add report history and a short resume preview you can delete from Reports or Settings.",
};

export const saveReportTrustMessage =
  "We only save reviews to verified signed-in accounts. Nothing is created in the background, and nothing gets handed off to an unverified email.";

export const extensionDisclosureMessage =
  "The extension only reads supported job pages when you ask it to capture a role. Sign-in is only required if you want synced history across devices.";

export const billingTrustMessage = {
  summary: "Stripe handles checkout, receipts, renewals, and cancellation.",
  support: "Support and restore paths stay visible before and after payment.",
};
